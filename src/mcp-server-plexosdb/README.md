# mcp-server-plexosdb

Standalone Model Context Protocol server package for plexosdb.

## Install

```console
uv add mcp-server-plexosdb
```

## Run

```console
uv run plexosdb-mcp
```

This package wraps the MCP server implementation from `plexosdb.mcp_server`.
